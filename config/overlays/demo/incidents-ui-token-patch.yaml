apiVersion: apps/v1
kind: Deployment
metadata:
  name: incidents-ui
spec:
  template:
    spec:
      containers:
        - name: ui
          env:
            - name: API_TOKEN
              value: test-admin-token
