apiVersion: v1
kind: Service
metadata:
  name: incidents-apiserver
spec:
  type: ClusterIP
  ports:
  - name: https
    port: 443
    targetPort: 8443
    protocol: TCP
  selector:
    app: incidents-apiserver
