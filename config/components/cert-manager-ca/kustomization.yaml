apiVersion: kustomize.config.k8s.io/v1alpha1
kind: Component

# Note: The base already includes certificate.yaml with ClusterIssuer, Certificate, and Issuer.
# This component exists for future extensibility but currently adds no resources.

resources: []
